  <% @schools.each do |s| %>
    <% if s == @selected %>
      <%= ">>" + s.school_name + "<<" %>
    <% else %>
      <%= link_to s.school_name, select_school_path(@user, format: :js, selected: s, prev_date: params[:start_date]), method: :get, remote: true %>
    <% end %>
  <% end %>